<?xml version="1.0" encoding="utf-8"?><!--
   Create your own accessibility service
   See https://developer.android.com/guide/topics/ui/accessibility/service
   for details.

   Specifies the types of Accessibility events the service listens to.
   "typeWindowStateChanged" means the service will listen to changes in window states,
   such as when the foreground app changes.
   android:accessibilityEventTypes="typeWindowStateChanged"

   Specifies the type of feedback the service provides.
   "feedbackGeneric" indicates the service does not provide any specific feedback
   and is only used internally for event processing.
   android:accessibilityFeedbackType="feedbackGeneric"
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="false"
    android:description="@string/app_name"
    android:notificationTimeout="100" />